#!/usr/bin/env bash
# =============================================================================
# CUSTOM STATUS LINE (alternative to the `statusLine` key in settings.json).
# The script runs and prints a single line shown at the bottom of the session.
# Session context (model, cwd, git branch, etc.) is provided as JSON on stdin.
# This example prints a static line; replace with real logic (e.g. parse stdin
# with jq and show branch + model). Keep output to ONE line.
# =============================================================================
echo "🌂 diploma-cloud-cyber-umbrella · cross-repo coordination"
