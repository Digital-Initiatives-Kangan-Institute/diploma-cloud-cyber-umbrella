# Step 06 — Scenario plan

*(per scenario — developed from the completed contract)* · Run-sheet: [process-assessment.md](../../../docs/process-assessment.md)

Author the scenario in two parts: Part 1 the narrative story bible (human-led), Part 2 a forward build checklist whose `SE-NN` items bind every `SR-*`.

## Tooling

- validator `validate_scenario_plan.py` — `.claude/skills/scripts/`
- format standard `scenario-plan-format.md` — `docs/`

## Gate

**Gate 06 → 07** · *validator* `validate-scenario-plan` = **PASS** — conforms, and every `SR-*` is satisfied with no phantoms.

## Status

**Not ported** — the tooling above still lives at its legacy location. Converting it is done on request,
test-first; see [factory/README.md](../../README.md).
