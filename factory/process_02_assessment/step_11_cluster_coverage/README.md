# Step 11 — Cluster coverage

*(capstone)* · Run-sheet: [process-assessment.md](../../../docs/process-assessment.md)

Prove the ATs together evidence every item in the consolidated UoC — the capstone to per-AT traceability.

## Tooling

- validator `validate_cluster_coverage.py` — `.claude/skills/scripts/`

## Gate

**Gate 11 → 12** · *validator* `validate-cluster-coverage` = **100%** — no missing required item, no phantom reference.

## Status

**Not ported** — the tooling above still lives at its legacy location. Converting it is done on request,
test-first; see [factory/README.md](../../README.md).
